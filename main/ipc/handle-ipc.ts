import { RunFile } from './handle-run';
import { closeWindow, minimizedWindow, restoreWindow } from './handle-win';

export const ipcHandle = {
	// handleOpenWeb,
	// handleLogin,
	// handleSearch,
	// handleFindImage,
	closeWindow,
	restoreWindow,
	minimizedWindow,
	RunFile
};
